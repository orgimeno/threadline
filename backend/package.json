{
  "name": "@threadline/backend",
  "version": "0.1.0",
  "description": "Threadline backend service.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "tsx --env-file=.env watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file=.env dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fastify/multipart": "^10.1.0",
    "ajv": "^8.20.0",
    "fastify": "^5.10.0",
    "openai": "^6.48.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.1",
    "typescript": "~6.0.2",
    "vitest": "^4.1.10"
  }
}
